# معماری نسخه ۳

## قواعد غیرقابل‌نقض

1. مبلغ اعشاری در پایگاه داده وجود ندارد؛ واحد پایه ریال است.
2. مقدار کالا با `QuantityMicros` ذخیره می‌شود و `Double` وارد هسته مالی نمی‌شود.
3. جمع بدهکار و بستانکار هر سند باید دقیقاً برابر باشد.
4. ثبت خرید، اقلام خرید، گردش انبار، ارزش انبار و سند حسابداری یک تراکنش اتمی است.
5. موجودی منفی، پرداخت بیشتر از مانده و تاریخ تسویه قبل از فاکتور ممنوع است.
6. ویرایش عملیات مالی با سند برگشت و سند اصلاحی انجام می‌شود؛ رکورد مالی حذف مستقیم نمی‌شود.
7. مرخصی تأییدشده و حضور یا غیبت در یک روز نمی‌توانند هم‌زمان ثبت شوند.
8. بازیابی بکاپ فقط پس از اعتبارسنجی کامل و داخل تراکنش جایگزینی انجام می‌شود.
9. سطح دسترسی هم در رابط و هم در لایه عملیات بررسی می‌شود.
10. هیچ عملیات پایگاه داده روی رشته اصلی رابط اجرا نمی‌شود.
11. فایل پایگاه داده با SQLCipher رمزگذاری و کلید آن با Android Keystore محافظت می‌شود.

## لایه‌ها

- `core`: پول، مقدار، تاریخ، نتیجه عملیات و اعتبارسنجی‌های مشترک
- `domain`: قواعد حسابداری، انبار، خرید، فروش، پرسنل و دارایی
- `data`: Room، DAOها، تراکنش‌ها، بکاپ و مرز همگام‌سازی
- `feature`: صفحه‌ها و ViewModel هر ماژول
- `app`: ناوبری، پوسته، تنظیمات و تزریق وابستگی ساده

## ماژول‌های محصول

- داشبورد و هشدارها
- خرید و تأمین‌کنندگان
- انبار، شمارش و ضایعات
- رسپی و بهای تمام‌شده
- فروش، مشتری و وصول
- حسابداری، روزنامه، کل، سود و زیان
- پرسنل، حضور، مرخصی، حقوق، پاداش، اضافه‌کاری و مساعده
- دارایی‌های رستوران
- کاربران و دسترسی مشاهده/ثبت/حذف/چاپ
- چاپ و خروجی
- تنظیمات، اعلان، بکاپ رمزگذاری‌شده و بازنشانی

## مسیر توسعه

### مرحله ۱: هسته

- پول ریالی، مقدار ثابت، حسابداری دوبل
- جداول پایه و تراکنش خرید/انبار
- تست مرزی و سرریز

### مرحله ۲: عملیات رستوران

- فروش و رسپی
- پرسنل و حقوق
- دارایی‌ها

### مرحله ۳: پایداری

- بکاپ رمزگذاری‌شده با Android Keystore
- اجرای دوره‌ای با WorkManager
- بازیابی و آزمون خرابی
- اعلان‌ها و چاپ

### مرحله ۴: آماده‌سازی شبکه

- Outbox محلی و شناسه جهانی
- API اختیاری VPS
- حل تعارض و سطح دسترسی سمت سرور
